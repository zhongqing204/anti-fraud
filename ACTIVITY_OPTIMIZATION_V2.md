# 反诈活动模块优化功能说明

## 📋 优化内容概览

本次优化包含以下功能：

1. ✅ **持续时间显示优化**：将"持续天数"改为"持续时间"，支持小时:分钟格式
2. ⏸️ **多天多时段活动支持**（预留扩展）：数据库已添加activity_schedule表
3. ✅ **退报申请与审批**：用户可申请退报，管理员审批后取消报名
4. ✅ **报名管理增强**：管理员可查看和管理所有报名用户信息

---

## 🔧 第一步：数据库更新

### 执行SQL脚本

在MySQL中执行：

```
D:\anti-fraud\backend\AntiFraud-backend\src\main\resources\sql\add_activity_schedule.sql
```

该脚本会：
- 创建`activity_schedule`表（支持多天多时段）
- 为`activity_signup`表添加取消报名相关字段：
  - `cancel_reason`：取消原因
  - `cancel_status`：取消状态（待审批、已同意、已拒绝）
  - `cancel_time`：申请取消时间

---

## ✨ 功能详细说明

### 功能1：持续时间显示优化

#### 修改位置
- 文件：`frontend/AntiFraud-frontend/src/views/user/ActivityDetail.vue`

#### 功能说明
- 原来显示"持续天数"（如：3天）
- 现在显示"持续时间"，格式更精确：
  - 少于1小时：显示"30分钟"
  - 1-24小时：显示"2小时30分钟"
  - 超过24小时：显示"3天5小时20分钟"

#### 示例
```javascript
// 2小时30分钟的活动
calculateDuration() → "2小时30分钟"

// 3天5小时的活动
calculateDuration() → "3天5小时"
```

---

### 功能2：退报申请与审批流程

#### 用户端操作流程

1. **查看报名状态**
   - 进入活动详情页
   - 如果已报名且审核通过，显示"申请退报"按钮

2. **提交退报申请**
   - 点击"申请退报"按钮
   - 填写退报原因（选填，最多500字）
   - 提交申请

3. **等待审批**
   - 提交后按钮变为"退报申请待审批"标签
   - 用户可在消息中心收到审批结果通知

4. **审批结果**
   - **同意**：报名记录删除，名额释放，用户收到通知
   - **拒绝**：保持报名状态，用户收到通知

#### 管理员端操作流程

1. **查看退报申请**
   - 进入"报名管理"页面
   - 表格新增"退报状态"和"退报原因"列
   - 有待审批的申请时，操作列显示"审批退报"按钮

2. **审批退报**
   - 点击"审批退报"按钮
   - 查看用户填写的退报原因
   - 选择审批结果：
     - 同意退报：删除报名记录，减少当前报名人数
     - 拒绝退报：保持报名状态
   - 填写审批说明（选填）
   - 提交审批

3. **查看审批历史**
   - 详情对话框中显示退报状态和原因
   - 可追溯所有退报申请记录

#### 消息通知

- **用户提交申请**：管理员收到消息通知
- **管理员审批完成**：用户收到审批结果通知

---

### 功能3：报名管理增强

#### 新增功能

1. **退报状态显示**
   - 列表显示每个报名的退报状态
   - 颜色区分：待审批（黄色）、已同意（绿色）、已拒绝（红色）

2. **退报原因查看**
   - 列表中直接显示退报原因
   - 详情对话框中完整展示

3. **一键审批**
   - 快速处理退报申请
   - 自动更新报名人数

#### 界面优化

| 列名 | 说明 | 宽度 |
|------|------|------|
| 用户名 | 系统用户名 | 100px |
| 真实姓名 | 报名时填写的姓名 | 100px |
| 手机号 | 联系方式 | 120px |
| 邮箱 | 电子邮箱 | 180px |
| 性别 | 男/女 | 60px |
| 年龄 | 用户年龄 | 60px |
| 单位/学校 | 工作或学习单位 | 150px |
| 活动名称 | 报名的活动 | 250px |
| 报名时间 | 提交报名的时间 | 180px |
| 审核状态 | 待审核/审核通过/审核拒绝 | 100px |
| 审核说明 | 管理员填写的说明 | 自适应 |
| **退报状态** | **待审批/已同意/已拒绝** | **100px** |
| **退报原因** | **用户填写的退报原因** | **自适应** |

---

## 📊 数据流程图

### 退报申请流程

```
用户提交退报申请
    ↓
设置cancelStatus = "待审批"
    ↓
发送消息通知管理员
    ↓
管理员查看申请
    ↓
管理员审批
    ├─ 同意 → 删除报名记录 → 减少currentParticipants → 通知用户
    └─ 拒绝 → 清空cancelStatus → 通知用户
```

### 报名人数同步

```
报名成功
    ↓
currentParticipants + 1

取消报名（待审核状态）
    ↓
直接删除 → currentParticipants 不变

退报申请审批通过
    ↓
删除报名记录 → currentParticipants - 1

管理员手动删除（审核通过状态）
    ↓
删除报名记录 → currentParticipants - 1
```

---

## 🎯 使用场景示例

### 场景1：用户临时有事无法参加

**背景**：用户张三报名了"社区反诈宣传活动"，已审核通过，但当天有急事无法参加。

**操作步骤**：
1. 张三进入活动详情页
2. 点击"申请退报"按钮
3. 填写原因："当天有紧急工作安排，无法参加活动，非常抱歉"
4. 提交申请
5. 等待管理员审批
6. 审批通过后，收到消息通知，名额释放给其他用户

### 场景2：管理员批量处理退报申请

**背景**：管理员李四发现有5个用户申请退报同一个活动。

**操作步骤**：
1. 进入"报名管理"页面
2. 筛选活动名称或退报状态
3. 逐个点击"审批退报"按钮
4. 查看用户原因，决定是否同意
5. 填写审批说明（如："理解，希望下次能参加"）
6. 提交审批
7. 系统自动更新报名人数和发送通知

---

## 🔐 权限控制

| 操作 | 用户 | 管理员 |
|------|------|--------|
| 查看活动详情 | ✅ | ✅ |
| 报名活动 | ✅ | ❌ |
| 取消报名（待审核） | ✅ | ❌ |
| 申请退报（审核通过） | ✅ | ❌ |
| 查看所有报名 | ❌ | ✅ |
| 审核报名 | ❌ | ✅ |
| 审批退报 | ❌ | ✅ |
| 删除报名记录 | ❌ | ✅ |

---

## 📝 API接口说明

### 用户端接口

#### 1. 申请退报
```
POST /activitySignUp/applyCancel
Body: {
  id: 报名记录ID,
  cancelReason: "退报原因"
}
Response: {
  code: "200",
  msg: "取消申请已提交，等待管理员审批"
}
```

### 管理员端接口

#### 2. 审批退报
```
PUT /activitySignUp/approveCancel
Body: {
  id: 报名记录ID,
  cancelStatus: "已同意" 或 "已拒绝",
  reason: "审批说明"
}
Response: {
  code: "200",
  msg: "审批成功"
}
```

---

## ⚠️ 注意事项

1. **只有审核通过的报名才能申请退报**
   - 待审核状态的报名可以直接取消
   - 审核拒绝的报名无需退报

2. **退报申请期间不能重复提交**
   - 已有"待审批"状态的申请时，不能再次提交

3. **审批同意后不可恢复**
   - 同意退报后会删除报名记录
   - 如需重新报名，需要再次提交报名申请

4. **人数限制自动同步**
   - 退报审批通过后，活动当前报名人数自动减1
   - 释放的名额可供其他用户报名

---

## 🚀 后续扩展建议

### 1. 多天多时段活动支持（已预留）

**现状**：
- 数据库已创建`activity_schedule`表
- 实体类`ActivitySchedule`已创建

**待实现**：
- 管理员端添加日程设置UI
- 后端实现日程CRUD接口
- 前端详情页显示活动日程列表
- 根据日程判断活动是否正在进行

**示例数据结构**：
```json
{
  "activityId": 1,
  "schedules": [
    {
      "scheduleDate": "2026-05-10",
      "startTime": "09:00:00",
      "endTime": "11:00:00"
    },
    {
      "scheduleDate": "2026-05-11",
      "startTime": "14:00:00",
      "endTime": "16:00:00"
    }
  ]
}
```

### 2. 候补报名功能

**功能描述**：
- 活动满员后，用户可加入候补队列
- 有人退报时，自动通知候补用户
- 管理员可手动调整候补顺序

### 3. 签到功能

**功能描述**：
- 活动现场扫码签到
- 统计实际到场人数
- 生成签到报表

### 4. 活动评价功能

**功能描述**：
- 活动结束后用户可评价
- 评分和文字反馈
- 管理员查看评价统计

---

## 📖 相关文件清单

### 数据库
- `add_activity_schedule.sql` - SQL脚本

### 后端
- `ActivitySchedule.java` - 活动日程实体类
- `ActivitySignup.java` - 报名实体类（已更新）
- `ActivitySignUpServiceImpl.java` - 报名Service（已更新）
- `ActivitySignUpController.java` - 报名Controller（已更新）

### 前端
- `user/ActivityDetail.vue` - 活动详情页（已更新）
- `admin/ActivitySignUp.vue` - 报名管理页（已更新）

---

## ✅ 测试检查清单

- [ ] 执行SQL脚本成功
- [ ] 重启后端服务
- [ ] 活动详情页显示持续时间（小时:分钟格式）
- [ ] 用户可提交退报申请
- [ ] 管理员可查看退报申请
- [ ] 管理员可审批退报（同意/拒绝）
- [ ] 审批同意后报名人数减少
- [ ] 消息通知正常发送
- [ ] 退报状态正确显示
- [ ] 边界情况测试（重复提交、状态校验等）
