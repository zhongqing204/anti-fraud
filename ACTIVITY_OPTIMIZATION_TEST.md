# 反诈活动优化功能测试指南

## 📋 优化内容概述

本次优化为反诈活动添加了以下功能：

1. **新增"未开始"状态**：活动状态从原来的2种（进行中、已结束）扩展为3种（未开始、进行中、已结束）
2. **自动状态管理**：根据活动的开始时间和结束时间自动设置状态，无需管理员手动选择
3. **报名人数限制**：管理员可设置活动最大参与人数，人数满后用户无法报名

---

## 🔧 第一步：数据库更新

### 执行SQL脚本

在MySQL中执行以下SQL文件：

```
D:\anti-fraud\backend\AntiFraud-backend\src\main\resources\sql\add_activity_participants.sql
```

该脚本会：
- 为`activity`表添加`max_participants`字段（限制人数）
- 为`activity`表添加`current_participants`字段（当前报名人数）
- 将现有数据的默认值设置为0（不限制）

---

## ✅ 第二步：功能测试清单

### 测试1：活动状态自动更新

#### 测试场景A：创建活动时的状态
1. 以管理员身份登录
2. 进入"活动管理"页面
3. 点击"新增"按钮
4. 填写活动信息：
   - 活动标题：测试活动-未来开始
   - 开始时间：设置为明天的时间
   - 结束时间：设置为后天的时间
   - 限制人数：50
5. 保存活动

**预期结果**：
- ✅ 活动列表中该活动状态显示为"未开始"（灰色标签）
- ✅ 表单中没有"活动状态"选择框（已移除）

#### 测试场景B：进行中的活动
1. 创建新活动：
   - 开始时间：设置为昨天的时间
   - 结束时间：设置为明天的时间
2. 保存后查看列表

**预期结果**：
- ✅ 活动状态显示为"进行中"（绿色标签）

#### 测试场景C：已结束的活动
1. 创建新活动：
   - 开始时间：设置为前天的时间
   - 结束时间：设置为昨天的时间
2. 保存后查看列表

**预期结果**：
- ✅ 活动状态显示为"已结束"（灰色标签）

#### 测试场景D：状态自动切换
1. 创建一个即将开始的活动（开始时间设为几分钟后）
2. 等待时间到达开始时间
3. 刷新活动列表页面

**预期结果**：
- ✅ 活动状态从"未开始"自动变为"进行中"

---

### 测试2：报名人数限制功能

#### 测试场景A：设置人数限制
1. 管理员创建新活动
2. 在"限制人数"输入框中输入：5
3. 保存活动
4. 在活动列表中查看该活动

**预期结果**：
- ✅ 列表显示"0/5"（当前报名/限制人数）

#### 测试场景B：不限制人数
1. 管理员创建新活动
2. "限制人数"保持为0或留空
3. 保存活动

**预期结果**：
- ✅ 列表显示"不限制"

#### 测试场景C：用户报名（未满员）
1. 以普通用户身份登录
2. 进入有名额限制的活动详情页
3. 查看报名情况区域

**预期结果**：
- ✅ 显示"已报名：0 / 5 人 (剩余 5 个名额)"
- ✅ "报名"按钮可用

4. 点击"报名"按钮，填写报名信息并提交

**预期结果**：
- ✅ 报名成功提示
- ✅ 活动详情页显示"已报名：1 / 5 人 (剩余 4 个名额)"

#### 测试场景D：人数已满时禁止报名
1. 使用5个不同的用户账号依次报名上述活动（限制5人）
2. 第5个用户报名成功后
3. 尝试用第6个用户账号报名

**预期结果**：
- ✅ 活动详情页显示"已报名：5 / 5 人 (已满员)"
- ✅ "报名"按钮显示"报名已满"且禁用（灰色不可点击）
- ✅ 如果强行调用API，后端返回错误："报名人数已满，无法继续报名"

#### 测试场景E：取消报名释放名额
1. 在人数已满的活动中，某个已报名用户点击"取消报名"
2. 确认取消

**预期结果**：
- ✅ 取消成功
- ✅ 活动当前报名人数减1（如：5/5 变为 4/5）
- ✅ "报名"按钮重新变为可用状态

---

### 测试3：前端界面显示

#### 管理员端（Activity.vue）

1. 进入管理员端"活动管理"页面
2. 查看活动列表表格

**预期结果**：
- ✅ 新增"限制人数"列，显示格式为"当前人数/限制人数"或"不限制"
- ✅ "活动状态"列正确显示三种状态，颜色区分：
  - 未开始：默认色（灰色）
  - 进行中：绿色
  - 已结束：灰色

3. 点击"编辑"某个活动

**预期结果**：
- ✅ 表单中没有"活动状态"下拉选择框
- ✅ 有"限制人数"数字输入框，最小值0，最大值99999
- ✅ 输入框下方有提示文字："设置为0表示不限制报名人数"

#### 用户端活动列表（user/Activity.vue）

1. 进入用户端"反诈活动"页面
2. 查看活动卡片列表

**预期结果**：
- ✅ 每个活动卡片显示活动类型标签（线上/线下）
- ✅ 如果有限制人数，显示"👥 当前/限制"
- ✅ 活动状态标签正确显示三种状态
- ✅ 筛选器中"状态"选项包含"未开始"

#### 用户端活动详情（user/ActivityDetail.vue）

1. 点击进入某个活动详情页
2. 查看活动时间信息区域

**预期结果**：
- ✅ 顶部显示活动类型标签（🌐 线上活动 或 📍 线下活动）
- ✅ 显示开始时间、结束时间、持续天数
- ✅ 线下活动显示地点，线上活动显示参与方式
- ✅ 底部显示"👥 报名情况"区域：
  - 有限制：显示"已报名：X / Y 人 (剩余 Z 个名额)"或"(已满员)"
  - 无限制：显示"已报名：X 人（不限制人数）"

3. 查看报名按钮状态

**预期结果**：
- ✅ 活动已结束：按钮显示"活动已结束"且禁用
- ✅ 人数已满：按钮显示"报名已满"且禁用
- ✅ 正常情况：按钮显示"报名"且可用

---

### 测试4：边界情况测试

#### 测试场景A：修改活动人数限制
1. 管理员编辑一个已有报名的活动
2. 原限制10人，当前已报名8人
3. 将限制改为5人

**预期结果**：
- ✅ 允许修改（后端不做校验）
- ✅ 前端显示"8/5"（超过限制）
- ✅ 新用户无法报名（因为当前人数已超过新限制）

#### 测试场景B：删除报名记录
1. 管理员进入"报名管理"页面
2. 选择一个活动的报名记录
3. 批量删除多条报名记录

**预期结果**：
- ✅ 删除成功
- ✅ 活动的当前报名人数相应减少

#### 测试场景C：时间为空的情况
1. 创建活动时不设置开始时间或结束时间

**预期结果**：
- ✅ 表单验证提示必填
- ✅ 如果绕过前端验证，后端默认状态为"进行中"

---

## 🐛 常见问题排查

### 问题1：状态没有自动更新

**可能原因**：
- 数据库字段未添加
- 后端服务未重启

**解决方法**：
1. 检查`activity`表是否有`max_participants`和`current_participants`字段
2. 重启后端Spring Boot应用
3. 刷新前端页面

### 问题2：报名人数没有增加

**可能原因**：
- 报名Service层代码未生效
- 数据库事务问题

**解决方法**：
1. 检查后端日志是否有错误
2. 确认`ActivitySignUpServiceImpl.add()`方法中包含更新`currentParticipants`的代码
3. 重启后端服务

### 问题3：前端显示异常

**可能原因**：
- 前端缓存未清除
- Vite开发服务器未重启

**解决方法**：
1. 清除浏览器缓存（Ctrl+Shift+Delete）
2. 重启前端开发服务器（npm run dev）
3. 硬刷新页面（Ctrl+F5）

---

## 📊 测试数据准备建议

为了全面测试，建议创建以下测试活动：

| 活动名称 | 开始时间 | 结束时间 | 限制人数 | 用途 |
|---------|---------|---------|---------|------|
| 未来活动 | 明天 | 后天 | 10 | 测试"未开始"状态 |
| 当前活动 | 昨天 | 明天 | 5 | 测试"进行中"状态和人数限制 |
| 历史活动 | 前天 | 昨天 | 不限 | 测试"已结束"状态 |
| 热门活动 | 昨天 | 下周 | 3 | 测试快速满员场景 |

---

## ✨ 功能亮点总结

1. **智能化状态管理**：系统自动根据时间判断活动状态，减少人工操作
2. **精细化人数控制**：管理员可精确控制活动规模，避免超员
3. **实时反馈机制**：用户端实时显示剩余名额，提升用户体验
4. **数据一致性保障**：报名/取消报名时自动同步更新活动报名人数

---

## 📝 后续优化建议

1. 可以考虑添加"候补报名"功能，当活动满员时用户可以加入候补队列
2. 可以添加报名截止时间，在活动开始前一定时间停止报名
3. 可以导出报名名单为Excel文件
4. 可以添加签到功能，统计实际到场人数
