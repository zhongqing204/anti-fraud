# 在线咨询功能说明

## 一、功能概述

在线咨询功能实现了用户与管理员之间的实时聊天通信，采用WebSocket技术实现双向实时通信。

## 二、核心特性

### 1. 消息广播机制

**用户发送消息：**
- 用户发送消息时，`toUserId = 0` 表示广播模式
- 消息会发送给**所有在线的管理员**
- 任意一个在线管理员都可以看到并回复

**管理员回复消息：**
- 管理员选择特定用户进行回复
- `toUserId = 用户ID` 表示私聊模式
- 只有该用户能收到回复

### 2. 多管理员场景处理

**问题：如果有多个管理员在线，会不会出现多人同时回复一个用户？**

**解决方案：**

#### 方案A：当前实现（广播+人工协调）
- ✅ 用户消息广播给所有在线管理员
- ✅ 所有管理员都能看到用户的咨询
- ⚠️ 需要管理员之间自行协调，避免重复回复
- 💡 **建议**：第一个看到消息的管理员快速响应，其他管理员等待

#### 方案B：改进建议（添加"正在输入"提示）
可以进一步优化，添加以下功能：
1. **管理员抢占机制**：第一个点击"回复"的管理员锁定该会话
2. **正在输入提示**：当某个管理员正在输入时，其他管理员看到提示
3. **会话分配**：系统自动将新用户分配给空闲的管理员

**当前采用方案A的原因：**
- 实现简单，易于理解
- 适合小规模团队（2-3个管理员）
- 管理员可以通过沟通避免重复回复

### 3. 聊天记录管理

#### 清空聊天记录

**用户端：**
- 点击"清空记录"按钮
- 确认后删除该用户与所有管理员的聊天记录
- 只删除自己的记录，不影响管理员端

**管理端：**
- 选择某个用户后，点击删除图标
- 确认后删除与该用户的所有聊天记录
- 只删除当前管理员的记录，不影响其他管理员

**注意：**
- 删除操作不可恢复
- 每个用户/管理员只能删除自己视角的聊天记录
- 数据库中的消息记录会被物理删除

## 三、技术实现

### 1. WebSocket连接

```
用户端: ws://localhost:8080/chat/{userId}
管理端: ws://localhost:8080/chat/{adminId}
```

### 2. 消息格式

```javascript
{
  fromUserId: 1,        // 发送者ID
  fromUserName: "张三",  // 发送者姓名
  toUserId: 0,          // 接收者ID（0表示广播）
  toUserName: "管理员",  // 接收者姓名
  content: "你好",       // 消息内容
  messageType: "text",  // 消息类型
  sendTime: "2024-...", // 发送时间
  isSelf: false         // 是否是自己发送的
}
```

### 3. 消息流转

**用户发消息流程：**
```
用户输入 → WebSocket发送(toUserId=0) → 后端保存数据库 → 广播给所有在线管理员
```

**管理员回复流程：**
```
管理员选择用户 → 输入回复 → WebSocket发送(toUserId=用户ID) → 后端保存 → 发送给该用户
```

### 4. 数据库设计

**chat_message表字段：**
- `from_user_id`: 发送者ID
- `to_user_id`: 接收者ID（0表示广播）
- `content`: 消息内容
- `is_read`: 是否已读
- `send_time`: 发送时间

## 四、使用场景示例

### 场景1：单管理员在线
```
用户A: "我想咨询反诈知识" 
       ↓ (广播)
管理员1: 收到消息
管理员1: "您好，请问有什么可以帮助您的？"
       ↓ (私聊)
用户A: 收到回复
```

### 场景2：多管理员在线
```
用户B: "我收到了诈骗短信"
       ↓ (广播)
管理员1: 收到消息 ✓
管理员2: 收到消息 ✓
管理员3: 离线 ✗

管理员1: "请提供短信内容，我帮您分析"  ← 第一个响应
       ↓ (私聊)
用户B: 收到管理员1的回复

【注意】管理员2看到管理员1已回复，就不再重复回复
```

### 场景3：清空聊天记录
```
用户C想清空记录：
1. 点击"清空记录"按钮
2. 确认操作
3. 前端清空消息列表
4. 后端删除数据库记录
5. 用户C的聊天界面变为空白

【注意】管理员端仍保留记录（除非管理员也删除）
```

## 五、最佳实践建议

### 对于管理员：
1. **及时响应**：看到用户消息后尽快回复
2. **避免重复**：发现其他管理员已回复，无需再次回复
3. **专业用语**：使用规范的反诈咨询话术
4. **记录重要信息**：对重要案例做好备注

### 对于用户：
1. **清晰描述**：详细说明遇到的情况
2. **耐心等待**：管理员可能在处理其他咨询
3. **保护隐私**：不要泄露银行卡密码等敏感信息
4. **定期清理**：适时清空不需要的聊天记录

## 六、未来优化方向

1. **智能分配**：根据管理员负载自动分配用户
2. **会话锁定**：防止多个管理员同时回复
3. **快捷回复**：预设常用话术模板
4. **满意度评价**：用户对咨询服务评分
5. **数据统计**：咨询量、响应时间等数据分析
6. **离线留言**：管理员离线时用户可留言

## 七、常见问题

### Q1: 为什么我的消息管理员没收到？
A: 检查管理员是否在线，WebSocket连接是否正常

### Q2: 多个管理员都回复我了怎么办？
A: 可以选择其中一个继续咨询，或综合多个管理员的建议

### Q3: 清空记录后还能恢复吗？
A: 不能，删除操作不可恢复，请谨慎操作

### Q4: 管理员能看到我与其他管理员的聊天吗？
A: 不能，每个管理员只能看到自己与用户的聊天记录

---

**版本：** v1.0  
**更新日期：** 2024年
